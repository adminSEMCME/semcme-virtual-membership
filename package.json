{
  "name": "semcme-virtual-membership",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/admin.js && node --check public/app.js && node --check data/program-sync.js",
    "test": "node tools/test-program-sync.mjs"
  },
  "engines": { "node": ">=22.5" },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "nodemailer": "^8.0.10"
  }
}
